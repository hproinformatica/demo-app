/* Data */
import { clearUserInfo, loadUserInfo, saveUserInfo } from './data/UserInfo'
import { clearSyncInfo, loadSyncInfo, saveSyncInfo } from './data/SyncInfo'
import { clearParamsInfo, loadParamsInfo, saveParamsInfo } from './data/ParamsInfo'

export default {
	clearUserInfo, loadUserInfo, saveUserInfo,
	clearSyncInfo, loadSyncInfo, saveSyncInfo,
	clearParamsInfo, loadParamsInfo, saveParamsInfo,
}
